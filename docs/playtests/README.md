# Playtests

Phase 6 treats playtests as a release-quality evidence stream.

- `PLAYTEST_TEMPLATE.md` is the per-session capture template.
- `FINDINGS_LOG.md` is the rolling findings log used by `tools/quality/peter_quality.py summarize-playtest`.
