# Data

Portable gameplay data lives here.

- `schemas/` defines JSON Schema contracts.
- `examples/` contains known-good sample content.
- `fixtures/invalid/` contains intentionally broken files for validation tests.
- `content/` contains runtime-loaded shipped content catalogs for rooms, encounters, missions, feedback tags, style profiles, and Phase 6 quality profiles.
