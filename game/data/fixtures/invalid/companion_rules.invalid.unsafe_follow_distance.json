{
  "kind": "companionRuleCatalog",
  "schemaVersion": 1,
  "rules": [
    {
      "id": "companion_rule.follow_distance.invalid",
      "chipName": "Stay Far Away",
      "priority": 50,
      "editableVariable": "follow_distance_meters",
      "defaultValue": 12.0,
      "minValue": 4.0,
      "maxValue": 10.0,
      "previewText": "Unsafe preview.",
      "effectDescription": "This should fail schema validation."
    }
  ]
}
