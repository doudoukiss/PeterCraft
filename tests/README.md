# Tests

This area owns cross-module automated checks.

- `unit/` validates portable rules and utility contracts
- `integration/` exercises multi-system behavior such as profile and telemetry flow
- `scenarios/` holds deterministic scenario harnesses
- `smoke/` verifies the boot shell launches and exits cleanly
